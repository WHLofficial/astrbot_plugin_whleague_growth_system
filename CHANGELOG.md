# 更新日志

本项目的版本号以 `metadata.yaml` / `config/defaults.py`（`PLUGIN_VERSION`）为准。

## [1.2.0] - 2026-09-17

### 新增：主场赛程联动（配合主营收插件 v2.6.0+）

- **只读桥** `services/revenue_bridge.py`：以 mode=ro 连接主营收插件的 `revenue_system.db`（自动定位 plugin_data，配置键 `revenue_db_path` 可覆盖），提供联赛状态 / 轮次 / 对阵查询；数据库缺失或损坏时全部静默降级，不影响成长系统任何既有功能。
- **赛程绑定**：`matches` 表新增 `rev_fixture_key / rev_side` 列（schema v4→v5 自动迁移），一条真实对阵在成长库最多两行（主/客视角各自 opponent=对面队名）；部分唯一索引保证同一对阵同视角不重复。普通比赛不受约束。经绑定通道保存时若已存在同日对手的旧记录会被采纳补绑，不会另开一行。
- **WebUI 第 8 页签「赛程」**：按轮次分组浏览真实赛程（支持文字前缀如「顶级9」，徽章显示第 N 赛季 · M 窗口与赛季名、每轮已打 x/y）；点开单场抽屉可见天气/比分，双边球员按 `players.team` 自动匹配进主客列（未匹配名单区手动指派），每行数据项输入框复用比赛页签渲染；保存走与命令一致的差值回放通道，首次保存可确认日期（默认今天）。浏览公开、录入修改需管理权限（WebUI 登录身份）。
- **推进提醒** `services/revenue_hooks.py`：监听主营收插件的窗口/赛季推进广播（经 `register_state_listener` 幂等注册），在推进发生的群里实时提醒「联赛进入新窗口/新赛季」，并附 `/成长 推进 <名称> [保留|清零]` 指引；配置键 `notify_on_league_advance`（默认开）。对方插件未升级到 v2.6+ 时安静跳过。
- **新公开子命令** `/成长 赛程 [轮次]`（别名 赛程表/对阵）：聊天侧快速查看当前窗口赛程。

### 编辑语义边界（重要）

- 仅**当前成长期**的数据可在 WebUI 修改；历史期数据锁定（结算不可变）。
- 一次性里程碑达成后不因数值降低而回撤——重复录入覆盖与文件重导为同一语义。

### 修复

- WebUI 赛程页签：轮次对阵列表错误解包 `fixtures` 响应（data 为 `{available, fixtures}` 对象却按数组取 `length`），导致每轮恒显「该轮暂无对阵」。
- WebUI 赛程页签：「录数据 / 查看 / 编辑」按钮无反应——`openDrawer` 未导入，点击时抛 ReferenceError。
- WebUI 赛程抽屉：`drawBody` 在 `rosters/apps/assigned` 声明前调用（TDZ ReferenceError），抽屉只渲染到「比赛日期」；且已录数据按行数组解析、与服务端按 player_uid 聚合的对象 shape 不符——统一改为直接采用服务端 shape，保存后原地重绘同步刷新花名册。
- WebUI 全量审查修复：`renderTable` 不向渲染函数传行号导致成长期结算表「#」列显示 NaN；比赛页球员下拉只加载第 1 页（10 人）改为翻页拉全量（上限 50 页）；配置页布尔项集合补 `notify_on_league_advance`（渲染为复选框）；清理球员页无效语句。
- WebUI 暗色主题：`html[data-theme="dark"] .btn` 的深色文字规则压过 `.btn.secondary` / `.btn.danger` 变体色，透明底按钮（成长期页签的「结算表」「推进成长期」等）在夜间变成深字叠深卡几乎不可见；改为 `--btn-primary-fg` token，变体色恢复。
- 赛程未按赛事分开：主场库轮次号按（赛季, 赛事）各自分配，联赛与杯赛的同号轮不是同一轮；桥接查询与聊天侧 `/成长 赛程` 均改为按（赛事, 轮次）聚合展示，并只显示当前赛季窗口（历史窗口不再混入）；文字轮次（如「顶级9」）改经 `round_names` 表解析为（赛事, 轮次号）。

### 变更：赛程「录数据」面板改版（按使用反馈重做）

- 抽屉改为**内嵌面板**：点击「录数据 / 查看 / 编辑」在对阵列表正下方展开录入卡片，不再弹出侧边抽屉；打开时平滑滚动到位，可随时「收起」。
- 去掉比赛日期输入：保存按当天落库（与聊天侧 `/成长上报` 同一差值回放通道；历史日期补绑请走聊天命令）。
- **主客切换**：面板顶部「主队 / 客队」两枚按钮，点选只显示该侧球员表（含各自人数），不再上下堆叠两侧；未匹配名单指派区保留在下方。
- **「出场」改为复选框**：自动识别规则中的出场计数项（键 `appearance` 或名称含「出场」，如 WHL 规则 `stat,appearance,出场,1`）——打过勾选（=1）、没打不勾（=0），已有记录回显勾选态；其余数据项仍为数字输入。
- **整表适配视口**：花名册表放入限高（460px）容器纵向滚动、表头冻结（sticky），数字输入宽度随列自适应、单元格紧凑化，任何常规视口宽度下无横向拖动；上方对阵列表同步精简——主队/比分/客队三列合并为「对阵」一列（7 列 → 5 列）。

### 变更

- `db/schema.py` SCHEMA_VERSION 4→5：存量库启动时自动 `ALTER TABLE` 补列并建部分唯一索引，幂等可重复执行。
- `_conf_schema.json` 新增配置键：`revenue_db_path`（留空自动定位）、`notify_on_league_advance`。

## [1.1.0] - 2026-09-15

### 新增：WebUI 管理台（pages/console）

- 按插件页面规范新增管理台页面：Dashboard → 插件详情 → 「成长系统管理台」即可打开（iframe 受限沙箱，经 bridge-sdk 调用插件 API；无需暴露额外端口）。
- 七个页签：总览（统计卡/近期比赛）、球员（名册搜索/排行榜切换/详情抽屉含经验刻度条）、比赛（按当前规则动态生成数据项的单场录入）、导入（上传→预览→确认三步流 + 待确认列表驳回）、成长期（历期汇总/结算表抽屉/推进二次确认）、规则（数据项分段计分与里程碑展示）、配置（按功能域分组行内编辑，复用聊天侧同一套校验强转）。
- 新增后端 `web_api.py`：18 个 REST 端点注册于 Dashboard 插件扩展通道（`/{PLUGIN_NAME}/...`），写操作入参统一走 `parse_num/parse_date/sanitize_*/validate_and_cast`，与聊天命令行为一致；导出文件经 `file_response` 直接下载。
- 页面视觉遵循「联赛秩序册 / 夜场记分牌」设计令牌：宋体标题、等宽数字、草皮绿主色、双主题跟随 WebUI（`data-theme`），窄屏自动折叠为顶部标签。
- `.astrbot-plugin/i18n/zh-CN.json` 注册页面标题与描述。

### 变更

- `db/dao.py` 新增 `search_players / recent_matches / count_overview` 三个查询（仅服务 WebUI 总览与搜索）。
- `main.py` 在 `initialize()` 末尾实例化 `WebApi`，随插件加载注册全部端点。

## [1.0.0] - 2026-08-22

命令面定稿版本（大规模上线基准）：自本版本起命令结构承诺稳定，新增能力走 1.x（新子命令），再破坏走 2.0.0。

### 破坏性变更（旧平铺命令移除）

- 命令面从 15 条平铺命令重构为**单一 `/成长` + 两级子命令**，v0.6.0 及之前的命令（`/成长规则`、`/成长查询`、`/成长排行`、`/成长球员`、`/成长期状态`、`/成长预览`、`/成长导入列表`、`/成长导入文件`、`/成长确认导入`、`/成长设置`、`/成长查看配置`、`/成长上报`、`/成长推进`、`/成长导出`）全部下线；旧写法会收到"命令已改版"提示并给出对应新用法，引导迁移。
- 新命令对照：`/成长 规则` · `/成长 查询 <球员ID|姓名>` · `/成长 排行 [页]|排行 生涯 [页]` · `/成长 球员 [页]` · `/成长 期 [期号]` · `/成长 上报 ...` · `/成长 推进 ...` · `/成长 导出 [期号]` · `/成长 导入`（列表）· `/成长 导入 <文件名>`（预览）· `/成长 导入 确认 <文件名>`（执行）· `/成长 配置 [键] [值]`。
- 子命令别名：`期 ← 期状态/预览`、`排行 ← 排名`、`球员 ← 名单`、`配置 ← 设置`；`/成长帮助` 仍指向帮助。

### 新增

- `/成长 查询` 支持按姓名查球员（UID 精确优先 → 姓名归一化精确 → 长度分级编辑距离容错，与比赛导入同套匹配器）；多个同名/相近球员时列出候选提示用 ID。
- `/成长 期` 无参输出并入原 `/成长预览` 的每人成长明细（当前期概况 + 每人 期初/期内获得/总经验/等级 + 近 5 期摘要），明细不分页（避免与期号参数歧义），长输出自动转合并转发卡片，防御性行数上限 500。
- `/成长 配置` 三合一：无参按功能域分组查看全部 26 个配置项（修复原"能设不能看"——查看配置只显示 4 键）；单键查看；`配置 <键> <值>` 设置（值可含空格）。
- `/成长 导入` 三合一：无参=待确认列表（原 `/成长导入列表`）；`导入 <文件名> [类型]`=预览挂起；`导入 确认 <文件名> [类型]`=执行（原 `/成长确认导入`）。
- 管理子命令鉴权统一在 `/成长` 分发器完成（此前各 handler 内检查、`成长导入列表` 在注册层检查，位置不一）。

### 变更

- 非法参数容错统一为报用法提示（原 `/成长排行`、`/成长球员` 非法页码静默回退第 1 页、`/成长期状态` 忽略多余参数，与 `/成长导出` 严格策略不一致）。
- handler 统一改为 `(event, args)` 签名（args 由分发器解析传入，不再各自解析 message_str）。
- 配置 schema `import_col_type` hint 修正为 stat/band/milestone/repeat/level（原漏 band/repeat）；README 配置说明补 `rank_page_size`、`import_require_confirm`。
- 群文件自动捕获与导入预览的确认提示统一指向 `/成长 导入 确认 <文件名>`。
- 回归测试增至 120 个用例（新增 18 个：分发器路由/别名/迁移提示/鉴权/白名单、查询姓名匹配、页码 usage、配置三分流、导入三分流、期明细截断；帮助与用法文案断言同步）。

## [0.6.0] - 2026-08-16

### 新增

- 比赛导入支持球员姓名：比赛文件（`比赛_*.csv/.xlsx`）球员列可填球员 ID 或球员姓名，UID 优先精确匹配，查不到转姓名匹配；姓名匹配带容错（归一化：小写去分隔符，内部缺空格/连字符均可命中；长度分级编辑距离：<4 字符仅精确、4~9 允许 1 处、≥10 允许 2 处字母差异）；命中多个同名/相近球员时该行报错"存在多个相近球员…请使用球员ID"（整批回滚）。命令 `/成长上报` 仍按球员 ID。
- 长反馈自动转发卡片：命令反馈纯文本行数达到阈值（默认 8 行，列表类反馈如排行/球员名单/成长期明细在手机 QQ 上一屏装不下会刷屏）时自动转为 QQ 合并转发卡片（每节点 ≤1500 字符、最多 50 节点，超出截断并提示可用 `/成长导出` 获取完整数据）；含图片/文件的消息链不转换；新增配置 `forward_threshold`（行数，0=关闭）、`forward_node_max_chars`、`forward_max_nodes`。

### 变更

- 新增 `utils/forward.py`（长文本按行分块 + 转发卡片构造）；全部 15 个命令反馈出口统一经 `_maybe_forward`。
- 回归测试增至 100 个用例（新增 15 个：姓名匹配/归一化/容错/多匹配、转发分块/阈值/截断/非纯文本跳过）。

## [0.5.1] - 2026-08-16

### 新增

- 计数型数据项：数据项单位经验可为 0（仅作计数，经验完全靠"每 N 次累计"或"单场达标"里程碑发放），适合"每 12 次夺回球权得 1 点"这类规则；区间段经验、里程碑/重复奖励、每级经验仍须为正数。

### 修复

- CSV/Excel 规则文件导入崩溃：`parse_rule_table` 的 cfg 参数在生产路径传入 `config_cache.get` 绑定方法（无 `.get` 属性），解析规则表时 `AttributeError`；改为兼容 dict 与配置读取函数两种形态。

### 变更

- 回归测试增至 85 个用例（新增计数型数据项解析/计算、cfg 绑定方法兼容、band/里程碑/每级经验仍拒绝 0）。

## [0.5.0] - 2026-08-16

### 新增

- 多数据项总和里程碑：若干数据项（如 进球+助攻）累计值之和达阈值，一次性奖励经验。JSON 用 `"stats": ["goal", "assist"]`，CSV/Excel 规则表的 stat 列用逗号分隔 `goal,assist`；支持成长期内（period）与生涯（career）累计，多数据项总和暂不支持每累计 n 次重复奖励（导入校验报错）。
- 单场达标额外经验：数据项单场值 ≥ 阈值（如评分 ≥9.0）额外奖励，每场判断、可重复；多条达标规则同场全部触发累加；milestone 行 period 列填 `match`、threshold 列填达标值即可（零新增列位）。区间型（bands）数据项也可配置，与区间经验叠加。
- 单场达标奖励随出场记录持久化（`appearances.bonus_xp`/`total_xp`，该列原恒 0），覆盖重录自动回收/补发（单场奖励是"场"的函数，重录按新值重算）。
- 反馈展示：上报反馈新增"单场达标"，多数据项里程碑显示 `进球+助攻` 汇总名；`/成长查询` 里程碑列表同步显示多数据项汇总名；`/成长规则` 展示多 key 累计合计与单场达标。

### 修复

- 里程碑触发反馈 KeyError：`_record_one` 追加的 awarded 条目键为 `stat`/`stat_keys`，上报反馈按 `m['stat_key']` 取值会崩溃（触发里程碑时），改为按显示名展示。

### 变更

- 规则模型里程碑新增 `match` 时期（单场达标）与多数据项形态 `stat_keys`（排序去重后逗号串存 `milestone_awards.stat_key`，幂等键不变）。
- 回归测试增至 81 个用例（新增 23 个：多 key/match 解析、计算、覆盖回收含规则变更、拒绝路径、展示）。

## [0.4.1] - 2026-08-15

### 修复

- 群文件捕获崩溃：`kind_from_name` 为模块级函数，但 `on_group_message`（群内发文件自动识别）与 `/成长导入文件`、`/成长确认导入` 未指定类型时均按实例方法调用，服务器收到群文件时触发 `AttributeError: 'GrowthImportService' object has no attribute 'kind_from_name'`。新增实例方法委托模块级函数，两处调用点行为不变。
- 回归测试增至 58 个用例（新增实例方法调用锁定，防止复现崩溃）。

## [0.4.0] - 2026-08-15

### 新增

- 成长期成长数据导出：`/成长导出 [期号]`（管理员）导出历史成长期或当前期每位球员的成长数据，字段为期初经验（= 上一期溢出结转，首期 0）、期内获得经验（= 期末总经验 − 期初，含里程碑/重复奖励）、期末总经验（结算前累计）、升级数、升级后剩余经验；无参时导出当前未结束成长期（升级数/剩余经验为"未结算"）。
- 导出文件格式优先级 Excel → CSV → TXT 自动降级：
  - Excel（openpyxl）：标题 + 期号/生成时间元信息 + 表头 + 斑马纹数据区 + 冻结窗格 + 汇总行，升级数列语义着色；
  - CSV：utf-8-sig 编码（Excel 打开中文不乱码）；
  - TXT：等宽对齐的文本表格。
- 文件发群优先，平台不支持自动发文件时保存到插件数据目录 `exports/` 并回复路径。
- `/成长预览`（公开）：群内文本预览当前未结束成长期的成长进度（期初/期内获得/当前总经验/等级），完整数据可请管理员导出。
- 推进成长期时自动导出：`/成长推进` 成功后自动生成刚结束成长期的成长数据文件发群，发群失败降级保存服务器，不影响推进结果。

### 变更

- 新增 `services/export_service.py`（ExportService：行数据组装、Excel/CSV/TXT 降级生成）。
- `period_snapshots` 明细查询补充球队字段；新增期内出场经验合计与全部活跃球员查询。
- 回归测试增至 57 个用例（历史期/当前期导出、期初结转、期内获得含里程碑、Excel 读回验证、降级链、保留/清零结转）。

## [0.3.1] - 2026-08-15

### 新增

- 成长期结果快照：推进成长期时记录每位球员的期末等级、升级数、本期经验与溢出结转（`period_snapshots` 表，历史可追溯）。
- `/成长期状态` 扩展：
  - 无参数：显示当前期信息、当前期累计总经验（进度）、历史成长期摘要（人数/升级人数/期末总经验）。
  - 带期号 `/成长期状态 <期号>`：查看该成长期每位球员的结果明细。

### 变更

- 数据库 schema v4：新增 `period_snapshots` 表（老库自动补建）。
- 回归测试增至 48 个用例（新增快照字段/摘要聚合/明细查询/清零策略结转）。

## [0.3.0] - 2026-08-15

### 新增

- 规则引擎扩展——区间型数据项（bands）：数据项按值命中的区间得固定经验（如评分 4.0~6.0 得 5、6.0~8.0 得 10、8.0 及以上得 20），未命中任何区间得 0 经验；区间为左闭右开，可开放上界，段间不得重叠；通用模型，任何数据项可配。
- 每累计 n 次重复奖励（repeat）：数据项每累计达 `step` 次奖励一次经验，可重复触发（区别于一次性里程碑）；成长期推进后重新累计。
- 经验值支持最多 1 位小数：线性单位经验、区间段经验、里程碑/重复奖励、`level_xp` 均可为 1 位小数；`step`（次数）仍为整数。
- 规则表新增 `band` / `repeat` 行类型（含 `import_col_band_min` / `import_col_band_max` 列位配置），无类型列自动推断。
- 新增 `repeat_awards` 表（重复奖励档位记录，幂等触发）。

### 变更

- 数据库 schema v3：经验相关 7 列（`players.xp/xp_total`、`appearances.stat_xp/bonus_xp/total_xp`、`milestone_awards.xp`、`repeat_awards.xp`）支持小数存储；老库无需重建（SQLite 动态类型），自动迁移版本号。
- `default_level_xp` 配置改为浮点（最多 1 位小数），推进结算改用整数运算（放大 10 倍）避免浮点误差。
- 经验展示统一 `fmt_xp` 格式化（整值显示如 `10`，小数显示如 `12.5`）。
- 区间型（bands）数据项禁止作为里程碑/repeat 的数据项（导入规则时校验报错）。
- 回归测试增至 45 个用例（新增 bands/repeat 解析与计算、小数经验、小数推进结算等）。

## [0.2.0] - 2026-08-14

### 新增

- 反馈文案人性化：
  - 新增统一提示工具 `utils/messages.py`（用法提示、权限提示、分级帮助、错误引导）。
  - 帮助命令 `/成长` 按权限分级：普通玩家仅见玩家命令，管理员额外见管理命令（含 `/成长设置`、`/成长查看配置`）。
  - 用法提示统一为 `用法: /成长<命令> <参数>`，带参命令附示例。
  - 错误提示统一加 `⚠️` 前缀并补充修正引导（日期/数值格式示例、数据项与球员不存在时给出查看命令、文件不存在时给出操作建议）。
  - 无规则、无球员数据等边界场景补充"下一步"引导。

### 变更

- `/成长导入列表` 改为仅管理员可见（原所有人可查看），与帮助分级一致。

## [0.1.0] - 2026-08-14

### 新增

- 规则导入：支持 JSON / CSV / Excel（含中文表头映射、无类型列推断、行级错误提示）。
- 球员库 / 比赛数据导入：CSV / Excel 批量导入，预览确认后写入。
- 成长经验计算：按规则将比赛数据换算为经验，同日期同球员重复上报覆盖重算。
- 里程碑：成长期 / 生涯累计达阈值一次性奖励，幂等颁发（每球员每里程碑仅一次）。
- 成长期推进：等级折算（逐级消耗、只升不降），溢出经验保留结转或清零。
- 13 个 `/成长` 命令：帮助、规则、查询、排行（本期/生涯）、球员名单、成长期状态、上报、推进、导入文件、确认导入、导入列表、设置、查看配置。
- 群文件捕获：管理员在群内发送 `规则_` / `球员_` / `比赛_` 前缀文件自动识别并预览。
- 配置面板（`_conf_schema.json`）：白名单、管理员、默认每级经验、推进默认策略、列位与导入安全参数。
- 回归测试：24 个用例（规则解析、经验计算、里程碑幂等、成长期推进、并发竞态等），配套 astrbot 桩与内存 SQLite。
